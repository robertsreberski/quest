Goal: Implement Quest init native agent template installation for Codex and Claude.

Stopping condition: npm test, npm run check:parity, npm run check:hygiene, npm run check:manifests, npm pack --dry-run, git diff --check, and codex debug prompt-input "noop" all pass or any unavailable live smoke is reported honestly.
